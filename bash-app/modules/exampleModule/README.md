# Example Module
## Structure
The module folder must have the module name
Complete the module.json with module infos
By default the entry point is main.sh
return a json that can be used by diag protocol